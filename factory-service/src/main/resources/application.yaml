spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    password: password
    username: postgres
  jpa:
    hibernate.ddl-auto: create-drop
    show-sql: true
  docker:
    compose:
      file: /home/rail/IdeaProjects/secondMicroservices/factory-service/docker/compose.yaml
      enabled: false
  application:
    name: factory-service
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 0
